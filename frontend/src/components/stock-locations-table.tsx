'use client';

import { StockLocationsData } from '@/types/stock-location-schema';

type StockLocationsTableProps = {
  stockLocations: StockLocationsData[];
};

export default function StockLocationsTable({
  pageSize
}: {
  pageSize?: number;
}) {
  return <div></div>;
}
